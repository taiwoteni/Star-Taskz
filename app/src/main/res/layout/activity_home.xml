<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:background="?attr/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".presentation.views.HomeActivity">
    <com.theteam.taskz.presentation.views.SplashRefreshLayout
        android:id="@+id/splash_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:animation="loading">
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">
                <androidx.viewpager2.widget.ViewPager2
                    android:id="@+id/view_pager"
                    android:layout_width="match_parent"
                    android:orientation="horizontal"
                    android:layout_height="match_parent"/>
                <!-- Intentionally did not include
                include bottom navigation bar menu items
                in XML file. This is because, inflating the menu
                corresponding to the user's account type
                will add more menus to the preset menu rather than
                override them-->
                <!-- Intentionally set the backgroundTint to ?attr/background
                because of Tasks Page
                -->
                <com.google.android.material.bottomnavigation.BottomNavigationView
                    android:id="@+id/bottom_nav_bar"
                    android:layout_width="match_parent"
                    android:layout_height="75dp"
                    android:layout_alignParentBottom="true"
                    android:layout_marginBottom="0dp"
                    android:foregroundGravity="bottom"
                    app:itemPaddingTop="12dp"
                    app:itemPaddingBottom="14dp"
                    android:theme="@style/BottomBarTheme"
                    app:itemIconSize="27dp"
                    app:itemRippleColor="@android:color/transparent"
                    app:itemIconTint="@drawable/bottom_navigation_item_selector"
                    app:itemTextColor="@drawable/bottom_navigation_item_selector"
                    android:background="@drawable/bottom_bar_rounded_top_background"
                    android:elevation="5dp" />
            </RelativeLayout>
        </FrameLayout>
    </com.theteam.taskz.presentation.views.SplashRefreshLayout>


</androidx.coordinatorlayout.widget.CoordinatorLayout>